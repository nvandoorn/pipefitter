{
  "name": "pipefitter",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.1.6"
  },
  "scripts": {
    "build": "tsc && cp bin/config.json build/bin",
    "build:run": "yarn run build && node build/bin/index.js"
  }
}
